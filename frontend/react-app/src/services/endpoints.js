const BASE_URL = 'http://127.0.0.1:8000/api/v1';

export const endpoints = {
    pbx: {
        ligacoes: `${BASE_URL}/pbx/ligacoes2/`
    },
    empresa: {
        colaboradores: `${BASE_URL}/empresa/colaboradores/`
    }
}
